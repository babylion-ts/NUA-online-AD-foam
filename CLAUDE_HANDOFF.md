# NUA 이미지 컨설팅 신청폼 — Claude 인수인계 문서

> 새 Claude 계정/세션에서 이어서 작업할 때 **이 문서를 가장 먼저 읽어 주세요.**
> 사용자는 한국어로 대화합니다. 마지막 정리: 2026-09-22.

---

## 1. 한눈에 보기

| 항목 | 내용 |
|---|---|
| 무엇 | NUA "Image Consulting 무료 후기 이벤트" 모바일 신청폼 (한 장짜리 `index.html`) |
| 라이브 주소 | https://nua-online-ad-foam.vercel.app/ (GitHub 저장소 → Vercel 자동 배포) |
| 응답 저장 | 구글시트 https://docs.google.com/spreadsheets/d/1hCSY5jYWKa0y7nh6fMzSMduKgbmQ7zZeknp94nbazFg/edit |
| 저장 방식 | `index.html` 의 `CONFIG.SCRIPT_URL` → Apps Script 웹앱(`apps-script.gs`) → 시트 `신청`/`로그`/`분석` |
| 원래 쓰던 스모어 폼 | https://smore.im/form/g7t2ebUcAw (전환 방식 레퍼런스) |
| 디자인 레퍼런스 스모어 폼 | https://smore.im/form/qrjbVhbbeC (letteRing — 버튼·입력칸·결과 화면 레퍼런스) |

⚠️ **라이브 사이트에는 이 문서의 변경 사항이 아직 올라가지 않았을 수 있습니다.** 사용자가 GitHub에 파일을 직접 업로드합니다(아래 7장).

---

## 2. 폴더 구조

```
image-consulting-form/
├─ index.html            폼 전체(HTML·CSS·JS 한 파일)
├─ apps-script.gs        구글시트 저장/이탈 분석 스크립트(구글 Apps Script에 붙여넣는 용도)
├─ README.txt            사람용 안내(배포·인수인계)
├─ CLAUDE_HANDOFF.md     이 문서
├─ pages/                이미지
│   ├─ (원본 — 지우지 말 것) p-1~p-9.jpg, process.jpg, done-base.jpg, done.jpg
│   │     사용자가 준 PDF를 1080×1800으로 렌더한 원본 페이지. 코드에서 직접 쓰는 건 p-1.jpg(공유 미리보기)뿐
│   ├─ (실제 사용) p-1n, p-2b, p-3x, p-4x, p-5n, p-6n, p-7q, p-8q, p-9q .jpg — 원본을 고친 사본
│   ├─ (실제 사용) m-<같은 이름>.png — 전환 조각 마스크
│   ├─ bg.jpg            요소를 뺀 공통 배경판(프로세스 페이지 배경 겸 전환 배경)
│   ├─ proc-s1a/s1b/s2a/s2b.jpg  프로세스 페이지 사진 4장(원본 process.jpg에서 잘라냄)
│   └─ done-48.jpg       완료 화면 배경
└─ tools/
    ├─ plate.png         배경판 무손실 원본(마스크 만들 때 기준)
    ├─ fx_mask.py        전환 조각 마스크 생성기(사용법은 파일 맨 위)
    └─ preview-server.py 로컬 미리보기 서버(구글시트 전송 차단 + 저장 시 자동 새로고침)
```

---

## 3. 핵심 구조 (반드시 이해하고 수정할 것)

### 3-1. 페이지 = 이미지 + 그 위 HTML
- 원래 디자인은 PDF 이미지. **디자인을 그대로 살리기 위해 페이지 이미지를 배경으로 깔고**, 누르는 곳은 투명 탭영역(`.hot`), 입력칸·버튼·글씨 일부는 HTML로 올린 구조.
- 모든 좌표는 **화면 %** (무대 = 1080:1800 비율). HTML 크기는 **폰 폭 390px 기준 px × `--u`**(`--u = 화면폭/390`)로 비례.
- `STEPS` 배열(= 페이지 순서):
  `intro → process → gender → pursuit → concern → mood → profile → phone → station → (제출) → 완료`

### 3-2. 스모어식 전환 (원래 스모어 폼의 'zoom' 전환을 코드에서 뽑아 재현)
- 평소엔 원본 모습 그대로(`.layer`), **넘어가는 순간에만** 배경판(`bg.jpg`) 위에 페이지를 "조각"으로 나눠 애니메이션.
- 조각 = 페이지 이미지를 마스크(`m-*.png`)로 오려낸 것 + HTML 요소 복제본. 정의는 `FX_PIECES`(이미지 조각, `r`=영역 %, `z`=연결된 탭영역 번호).
- 값: 나감 scale→.1·투명 0.8s(줄마다 70ms 간격) / 들어옴 .3→1, 0.42s 뒤 95ms 간격, `cubic-bezier(.19,1,.22,1)`. 선택 시 고른 것 1.04배·나머지 .96배+흐림(0.18s). 첫 화면은 떠오름(fadeInUp), 첫 화면을 떠날 땐 왼쪽으로 밀림. 약관 팝업·완료 화면도 스모어 값.
- **페이지 이미지를 바꾸면 반드시**: ① 마스크 다시 만들기(`tools/fx_mask.py`) ② `FX_PIECES` 좌표 갱신. 안 하면 넘어갈 때 옛 모습이 번쩍임.
- 배경판(`plate.png`/`bg.jpg`)은 여러 페이지가 픽셀 단위로 같은 배경을 공유해서 만들 수 있었음. 가운데 띠(y 42~60%)와 위쪽(2~9.5%)은 추정값이라 원래 배경과 살짝 다름(마스크가 그 차이까지 조각에 포함해 보정).

### 3-3. 페이지별 현재 상태
| 페이지 | 이미지 | 위에 올린 HTML / 특징 |
|---|---|---|
| intro | p-1n (그려진 '신청하기' 지움) | 미니멀 '> 신청하기' 버튼(가로만 150px) |
| process | bg.jpg | **전부 HTML**(`blocks`): 세로 타임라인(시안 2-2), 제목 SUIT, 설명 고운돋움, 사진 proc-*.jpg, '+ 결과지 PDF' 칩 |
| gender | p-2b ('다음' 지움) | 남/녀 누르면 바로 다음(auto) |
| pursuit | 여 p-3x / 남 p-4x (질문 1.5%↓, 사진 아래 이름 지움) | 이름은 HTML `.plabel`(Pretendard 10.5px, 흰72%) — 사진 조각 안에 넣어 함께 움직임 |
| concern | p-5n (사진 폭 80%·모서리15px, 질문 사진 아래, 선택지 그림 지움, 묶음 세로 가운데) | 선택지 4개 = 미니멀 스모어 버튼(`.smore-btn.choice`, 36px, 13px) |
| mood | p-6n (숫자 줄·라벨·다음 지움) | 스모어 PC식 0~10 숫자 버튼(`.scale-pc`), 같은 숫자 재클릭 = 해제 |
| profile | p-7q (질문 78%, 서브 줄 크기 통일·1.5%←) | 스모어 유리 입력칸 2개(`.field.glass`, 시안 A2) |
| phone | p-8q (질문 78% 가운데) | 유리 입력칸 폭을 문장 길이에 맞춤, '010-' 자동·하이픈 자동 |
| station | p-9q | 유리 입력칸 + 개인정보 동의(필수) → 제출 |
| 완료 | done-48.jpg (사진 위 크게, 아래는 같은 사진 흐리게) | '신청 완료' 테두리 배지 → '감사합니다'(SUIT 600) → 짧은 선 → '추후 유선으로 안내드립니다 :)'(고운돋움) → 맨 아래 '@nua.look'(인스타 링크, '👆 눌러서 인스타그램 보기' 깜빡임) |

공통 '다음' 버튼: 미니멀 스모어 버튼(`.smore-btn.next`, 96×36px@390, 흰76%, '>' 아이콘, 필수값이 비면 반투명). 선택 시 생기던 주황 테두리는 **사용자 요청으로 전부 제거**.

### 3-4. 글씨체
Noto Sans KR(기본) · SUIT(제목류) · 고운돋움(설명류) · Pretendard(작은 라벨·칩). Google Fonts + jsDelivr에서 로드.

---

## 4. 구글시트 연결 (`apps-script.gs`)
- 폼이 보내는 이벤트: `view`(페이지 볼 때) / `answer` / `leave` / `policy_open` / `submit`. 세션 id(`sid`) 단위.
- 시트: `신청`(최종 제출 목록) · `로그`(모든 이벤트 원본) · `분석`(단계별 도달/이탈 퍼널, 제출 때마다 자동 갱신 + 메뉴 '📊 이탈 분석').
- 스크립트를 고치면 사용자가 Apps Script 편집기에 붙여넣고 **배포 → 배포 관리 → 새 버전**으로 다시 배포해야 반영.
- 참고: 분석 퍼널(`STEPS`)에는 'process' 단계가 빠져 있음(원하면 추가).

---

## 5. 작업·검증 방법
- **미리보기:** `python3 tools/preview-server.py` → http://localhost:4190
  - 응답에 스크립트를 끼워 **script.google.com 전송을 막음**(미리보기로 눌러도 실제 시트가 오염되지 않음). 콘솔에 '[미리보기] 시트 전송 차단'.
  - index.html/pages 저장 시 자동 새로고침.
  - (원래 PC에서는 `claude form/.claude/launch.json`에 `image-consulting` 설정으로 등록돼 있었음.)
- **움직이는 중간 모습 확인:** 브라우저 스크린샷은 애니메이션 중 옛 화면을 줄 수 있음 → JS로 클릭 후 `document.getAnimations().forEach(a=>a.pause())`로 멈추고 캡처.
- 헤드리스 크롬으로 `file://` 열면 `mask-image`가 막혀 조각이 안 보임(http로 열 것).
- 이미지 수정 원칙: **원본 p-*.jpg는 덮어쓰지 말고 새 이름으로 사본** 만들기. 지운 자리는 배경판/주변 배경으로 메움(이음새 오차 확인).

---

## 6. 사용자와 일하는 방식 (지금까지 확인된 선호)
- 한국어, 짧고 명확하게. 결과를 옆 미리보기로 바로 보여주는 걸 선호.
- 디자인 변경 요청은 보통 **"시안 N개 뽑아줘" → 번호 선택 → "크게 보여줘" → 적용** 순서. 시안은 한 장 이미지(그리드)로 만들어 보내면 됨. 번호는 이어서 매김(예: 11~20).
- 스모어 레퍼런스는 **"똑같이"** 를 중시 → 스모어 CSS/JS(값)를 실제로 뽑아 재현했음. `https://smore.im/static/template/dist/` 의 main.js·청크(829번에 폼 컴포넌트)·CSS에서 확인 가능.
- **실제 스모어 폼을 클릭해 넘기지 말 것**(남의/사용자 폼 응답·이탈 통계가 오염됨). 페이지 HTML의 `doda-data`(JSON)와 코드만 읽기.
- 미니멀·고급스러운 톤 선호. 크기 조정 요청이 잦음("조금 더 작게/아래로" 등) — 작은 단위로 조정.
- 이미지·디자인은 요청 없이 바꾸지 말 것.

---

## 7. 배포 (라이브 반영)
1. GitHub 저장소 → Add file → Upload files → `index.html` + `pages/` 폴더(필요한 파일) 드래그 → Commit
2. Vercel이 약 1분 뒤 자동 재배포(주소 유지)
- 라이브에 필요한 파일: `index.html`, `pages/` 안의 **실제 사용 파일 전부**(2장 표의 '실제 사용' + `p-1.jpg`). `tools/`, 이 문서, README는 올리지 않아도 됨(올려도 무해).
- 이번 작업으로 라이브엔 아직 안 올라갔을 가능성이 큼 → 사용자에게 폴더째 업로드 권하기.

---

## 8. 남은 일 / 알려진 이슈
- [ ] 최신 파일 GitHub 업로드(위 7장).
- [ ] 개인정보 동의 안내문(`POLICY` 상수)의 수집 항목이 "이름, 전화번호, 주소"인데 실제로는 성함·출생연도·연락처·가까운 역을 받음 → 문구 확인 필요(처음에 한 번 알렸고 아직 미수정).
- [ ] 공유 미리보기 `og:image`가 상대경로(`pages/p-1.jpg`) → 카톡 등에서 안 뜰 수 있음. 절대 URL로 바꾸면 좋음.
- [ ] Apps Script 분석 퍼널에 'process' 단계 추가(선택).
